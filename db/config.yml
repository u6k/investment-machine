default: &default
  adapter: postgresql
  encoding: utf8
  database: <%= ENV["DB_DATABASE"] %>
  host: <%= ENV["DB_HOST"] %>
  username: <%= ENV["DB_USERNAME"] %>
  password: <%= ENV["DB_PASSWORD"] %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default

